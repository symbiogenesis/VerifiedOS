# Static memory without an MMU: capacity limits and a research agenda

> Non-normative research companion. The [requirements register](../requirements-register.md) remains authoritative. This document identifies limits, surveys research, and proposes experiments; it changes no admission criterion or architectural exclusion. Literature status is checked through 13 September 2026. Proposed conjectures below are this document's research questions, not established results or claims made by the cited authors.

Static allocation makes memory use explicit and bounds interference. It does not make every unused byte available to every request. A useful research objective is to **increase the workload a fixed physical memory budget can admit, while retaining bounded execution, capability isolation, and no address translation**. Zero waste is too broad a target: some reservation is the cost of an unconditional service guarantee, some is required for safe reuse, and some is avoidable layout or representation overhead.

The most promising first work is offline: improve physical placement, change object lifetimes and representations, and jointly schedule computation and reclamation. More flexible sharing and movement deserve separate experiments with their changed assumptions visible. Neither a new allocator nor a new MMU is a prerequisite for studying the first group.

## What remains valuable

The [static memory plan](../spec.md#r-08-010) gives every object a bounded home before execution. The [capacity contract](../spec.md#r-08-045) refuses an image whose physical reservations do not fit; runtime occupancy remains variable, and a [full pool](../spec.md#r-08-047) can decline service. This is a useful separation of admission from operation, even when utilization is poor.

The design's retained advantages have different causes. Removing online placement removes its search, allocator state, and synchronization costs. Fixed ownership prevents one compartment's burst from consuming another's reservation. CHERI bounds supply access control without page translation; the absence of page tables, TLBs, faults for demand paging, and shootdowns removes their state and proof obligations. The fixed memory classes and fabric schedule support timing analysis. Exact slot plans expose initialization, revocation, and capacity costs to checking before deployment. No single one of these properties establishes all the others: in particular, an MMU-free system can have a dynamic allocator, and a statically allocated system can have wasted space.

Keeping these advantages is the baseline for comparison. A capacity gain is useful only after charging its additional instructions, metadata, zeroization, reclamation, memory traffic, deadlines, and proof obligations.

## What fragmentation and stranded capacity mean here

"Free" needs an owner, a size, a location, a time, and permission to reuse. A byte holding no useful payload can still be unavailable to the request under consideration. This is usually **stranded capacity**, not a lost-pointer memory leak. Safe revocation deliberately makes old authority unusable; eventual reuse is a separate property.

| Cause | How capacity becomes unavailable | What could improve it |
| --- | --- | --- |
| Physical packing gaps | Contiguous objects with overlapping lifetimes constrain where other objects fit, even in an offline plan | Better packing; a different schedule or object representation |
| Alignment and bounds granularity | Object bases and extents satisfy native access, tag, and exactly representable capability bounds | Layout and representation changes with exact narrowing retained |
| Fixed size classes | A small payload occupies a larger slot; one class fills while another is empty | Compose different classes; bounded segmented representations |
| Peak reservation | Backing remains reserved while a pool is below its admitted peak | Prove mutually exclusive use, stream state, or change the promised service |
| Ownership and placement restrictions | A different island, pool, memory class, or bank has space the requester cannot use | Recompose ownership or placement; runtime borrowing needs a different contract |
| Extended retention | A region, saved state, alias, or asynchronous operation keeps storage unavailable after its last useful computation | More precise ownership transitions; smaller regions; earlier completion |
| Quarantine and recovery | Retired objects await containment, sweep and initialization; restart workspaces require room during failure | Jointly budget retirement bursts, sweep service and recovery overlap |
| Duplicate contents | Independently owned mutable state happens to contain the same data | Statically justified sharing or recomputation; value-based runtime deduplication changes assumptions |

These categories are diagnostic, not an additive accounting formula. A quarantined slot can also contain alignment padding. A measurement needs a disjoint byte ledger and may attach several explanations to the same byte; adding independent estimates double-counts it.

Consider a deliberately synthetic composition with two owners, each holding a pool of four 64-byte slots. If one pool is full and the other empty, a fifth request to the first owner fails despite idle backing at the second. No online allocator has created a hole, and no pointer is lost. The existing no-borrowing rule makes the unused capacity unreachable to that request. More accurate placement within either pool cannot fix this example.

Likewise, two free 32-byte extents separated by a pinned live object do not satisfy one contiguous 64-byte request. This is an example of a particular layout, not proof that the best offline layout must have that gap. The distinction between a poor assignment and an unavoidable optimum matters.

### Live payload is a lower bound, not a placement theorem

For one fixed execution schedule and one allocatable address arena, let each object have size `s_i` and lifetime `[b_i, e_i)`. Define:

```text
L = max_t sum of s_i over objects live at t
OPT = minimum span of a legal, fixed, contiguous placement
P = span of the particular legal placement emitted by a planner

L <= OPT <= P
```

The inequalities assume the same objects, lifetime endpoints, and legal constraints in all comparisons. `OPT - L` is the unavoidable gap for that model; `P - OPT` is planner suboptimality. Neither equals total platform overhead. Apply the model separately to physically distinct arenas; adding capacities from different memory classes does not make their bytes interchangeable. For a real image, lifetimes extend through safe reuse, and metadata and recovery reservations belong in the charged model.

General offline dynamic storage allocation can require `OPT > L`. Buchsbaum, Karloff, Kenyon, Reingold and Thorup study precisely this separation and provide approximation results, including a polynomial-time `(2 + epsilon)` approximation in the general problem. That result assumes the paper's rectangle/interval model; it is neither equality with live load nor a guarantee for an arbitrary greedy planner with added CHERI, island, and bank constraints. [*OPT Versus LOAD in Dynamic Storage Allocation*, SIAM Journal on Computing, 2004](https://epubs.siam.org/doi/10.1137/S0097539703423941)

### What the classical approximation frontier can bound

The classical DSA instance has known sizes and fixed time intervals, one unbounded address strip and freely chosen contiguous bases. Its hardness and approximation frontier matter here because extra reserved span consumes physical capacity, but a capacity ratio becomes die area only after the selected memory macro, metadata, redundancy and layout costs are accounted for. The paper also bounds `OPT/L` as `1 + O((h_max/L)^(1/7))`, with `h_max` the largest object; that asymptotic relation is distinct from an algorithm's approximation ratio against `OPT`. Neither is a numerical tapeout allowance without the theorem's constants, instance premises and an admissible construction.

Two questions must stay separate: how closely a polynomial-time algorithm can approach `OPT`, including the general PTAS question, and the extremal gap between `OPT` and live load. The [2020 packing-problems lecture](https://people.iith.ac.in/caldam2020/talks/school-arindam.pdf) lists breaking DSA's factor-two approximation barrier as open. This research tracks that frontier; it adopts no general PTAS or tight universal `OPT/L` value.

The [structural experiment](../implementation/static-memory/structure.md) addresses a restriction of classical DSA: distance from laminar lifetimes measured by minimum deletion number `k`. Its five-cycle **crossing** witness has `k = 3` and a load gap; its `k = 2` equality and fixed-parameter tractability questions are narrower questions, not equivalent reformulations of a general PTAS. Crossing and interference are different graphs. The PARTITION reduction with fixed obstacles additionally restricts bases, so its hardness cannot by itself settle the freely movable DSA instance or the parameterized question.

For Q5b comparisons, record the exact theorem model beside any theoretical ceiling. Removing platform constraints gives a relaxation and hence a lower bound on the constrained optimum; a placement or upper bound in that relaxation is not an upper bound for the real image. Charge alignment and representability rounding explicitly, retain separate arenas and owner/bank restrictions, and check each emitted candidate through the existing placement checks. A theoretical reference and measured heuristic spans answer different questions; neither replaces the other's evidence. No new optimizer or checker is authorized by citing an approximation theorem.

Bin packing is adjacent but also has a different objective: minimizing the number of identical bins for a list of items. Hoberg and Rothvoss give a randomized expected-polynomial algorithm within `O(log OPT_f)` bins of the Gilmore-Gomory fractional optimum and discuss its unresolved constant additive gap. That line does not directly bound R-08-018b's rounding waste inside fixed size-class slots: bins may mix item sizes, while this platform also fixes class reservations, lifetimes and ownership. Any transfer needs an explicit reduction preserving those constraints and must distinguish the integral-optimum additive-algorithm question from the LP integrality-gap question. [*A Logarithmic Additive Integrality Gap for Bin Packing*, SODA 2017](https://arxiv.org/abs/1503.08796)

### Exact special cases and quantifier scope

A useful exact special case is a fixed family of laminar object lifetimes: any two are disjoint or one contains the other. With fixed sizes, one arena, and no extra alignment or pinning restrictions, placing each object above its ancestors achieves the maximum simultaneous load. This elementary stack argument concerns those object lifetimes. Nested region scopes do not establish that all objects inside them die at their last use, and changing arbitrary lifetimes into nested ones can increase retention. Tofte and Talpin's region calculus establishes a safe region discipline with runtime allocation into regions; it does not establish a fixed physical slot and a tight whole-program capacity bound for every allocation. [*Region-Based Memory Management*, 1997](https://www.sciencedirect.com/science/article/pii/S0890540196926139)

Ownership alone also does not determine exact sizes, allocation multiplicity, or elapsed lifetime. An exclusively owned list can grow with input; an exclusively owned buffer can remain live until an external response. A composition needs explicit bounds and checked control-flow or protocol premises. A workload trace can suggest useful transformations, but one trace is not evidence that two reservations never overlap on any admitted execution.

There is another gap between one trace and all inputs. Suppose `N` unit-sized object identities each have one immutable offset, and the allowed modes activate any pair but never three. Every mode's peak payload is two units, yet every pair needs distinct offsets because it coexists in some mode. One layout valid for every mode therefore needs `N` units. This elementary example does not describe every possible pool implementation: generic reusable slots or different precomputed bindings can avoid its fixed-identity premise. It shows why the research must specify whether it optimizes one known trace, a conservative interference graph over all executions, or a finite family of checked bindings.

### Requirement scope and the remaining joins

R-08-012 distinguishes the absence of online packing from static placement gaps and states charged peak as a lower bound on legal span. Equality needs R-08-013's laminar, common-alignment and unrestricted-base premises or a checked placement with a matching lower bound. R-08-011 requires exported reservation intervals to cover every admitted execution, including authorized reuse; ownership and region names alone supply no such theorem. R-08-018's equal-slot pool has no variable-sized holes *within that pool*; R-08-018b accounts for size-class waste, R-08-019 for peak-versus-average capacity, and R-15-007k for quantized placement. All of these charges remain visible in the capacity report.

The [baseline](../implementation/static-memory/baseline.md) states the proof assumptions and counterexamples for these distinctions. The [memory-plan specification](../../proofs/MemoryPlan.v) checks interference against exported endpoints; sound extraction of those endpoints remains a compiler or composition-exporter obligation. The [placement-search contract](../implementation/placement-search.md) distinguishes live footprint from address span and padding, and refuses a footprint-gain claim from moving bases alone. Its witness plan is not a measured product roster.

[R-08-014's interference predicate](../implementation/placement-search.md#4-the-constraints-and-who-decides-them) requires physical disjointness when reservation lifetimes overlap. Disjoint lifetimes may share backing after the complete reuse gate. The feasibility check does not establish that gate or planner optimality.

## Application lifecycle and workflow profiles

The [workflow and hibernation contract](../implementation/workflow-profiles.md) carries the 19 September 2026 follow-up literature review and Q32's qualification scope. Frozen residency saves execution but retains memory. Semantic checkpoint, complete authority retirement and sanitization can release an app's private working set into a prechecked slate binding; immutable code, checkpoint storage, services, quarantine and transition scratch remain charged. Joint optimization must include every transition and repeated mode changes, not only endpoint peaks. The revocation mechanism remains required; a general online heap allocator is deferred pending a concrete fixed-pool/region failure under the same workload.

## Research with a plausible path to this design

The sources below supply methods and counterexamples. Their measured gains belong to their workloads and machines; this document transfers none of those percentages to VerifiedOS.

### Certified offline placement and joint scheduling

**Established method, unfinished integration.** OLLA jointly optimizes tensor lifetimes and physical locations using an integer program. Its useful lesson is that changing execution order can reduce live load while moving addresses alone cannot. Applying this to VerifiedOS requires preserving data dependencies, observable I/O order, functional results, and each admitted slot bound; training-graph freedom is not permission to reorder arbitrary operating-system effects. This work carries two names: the arXiv posting is the OLLA preprint, and the peer-reviewed version is *MODeL: Memory Optimizations for Deep Learning*, ICML 2023, PMLR 202:32618-32632, so a claim resting on review cites the latter. Nothing in either is machine-checked, and optimality holds only against the authors' own integer-program encoding and only where the solver proves it. The implementation the preprint names was renamed and is archived read-only by its owner: it is a readable MIT-licensed reference, not a maintained tool. [Steiner, Elhoushi, Kahn and Hegarty, *OLLA* preprint](https://arxiv.org/abs/2210.12924), [the MODel_opt implementation](https://github.com/facebookresearch/MODel_opt)

**Recent empirical candidate.** Lamprakos et al.'s *Futureproof Static Memory Planning* develops idealloc for large offline instances, including difficult cases beyond small tensor examples. The April 2025 version is a preprint submitted to TOPLAS, still at v1 with no journal reference, so it remains a preprint and not a reviewed result. It motivates scalable heuristics and a harder benchmark corpus, but its empirical success does not certify optimality or the feasibility of a VerifiedOS placement. The implementation is MIT-licensed and is not maintained: its last push is July 2025, and its own README disclaims both peer review and code quality. The preprint text is separately CC BY-NC-ND 4.0, a no-derivatives licence, so it may be cited and linked but not adapted; any description of the algorithm this project writes is written independently of that text. [Lamprakos, Xanthopoulos, Katsaragakis, Xydis, Soudris and Catthoor, *Futureproof Static Memory Planning*](https://arxiv.org/abs/2504.04874), [the idealloc implementation](https://github.com/cappadokes/idealloc)

The first experiment reuses Q5's exporter and independent candidate checks. Compare existing enumeration with deterministic heuristics, then consider a solver only for a demonstrated coupled problem. An untrusted search can emit a checkable placement; proving optimality additionally needs a lower bound or an independently checkable infeasibility argument. Solver timeout means the search is unfinished. The current objective still preserves footprint before locality and changes no island assignment. Q5 conditionally discusses a solver, while R-05-104's criterion says no ILP machinery exists in the toolchain; any proposed solver integration needs that scope tension resolved first.

The systems that tension is about are identifiable, and they differ in what each one actually checks and in which prover checks it. FznDrcpCheck checks a DRCP proof against a FlatZinc model and is proved sound in Rocq, with the FlatZinc grammar in its specification and the OCaml extraction and compiler in its trusted base, while its DRCP parser is deliberately outside that base; it is peer-reviewed and MIT-licensed. CakePB checks elaborated kernel-format pseudo-Boolean proofs, is proved sound in HOL4, is compiled by the verified CakeML compiler and is BSD 3-Clause; VeriPB, the proof format and the elaborating checker that feeds it, is itself unverified, so the two names are not interchangeable. LeanCSP and PBLean check in Lean and are both preprints; PBLean's scalable path adds Lean's compiled-code axiom to its trusted base, and its kernel-only path does not scale. LRAT is the oldest leg, a proof format with certified Coq and ACL2 checkers whose terms differ: the ACL2 checker is 3-clause BSD from its own file headers, while the Coq checker is a bare tarball from a university page that states no licence at all, so its terms are unread and reading them means opening that archive or asking its authors. The recent route importing LRAT certificates into Lean theorems is a preprint whose default mode carries that same compiled-code axiom. SCIP's exact mode solves rational mixed-integer linear programs and writes a VIPR certificate covering the branch-and-bound reasoning but not presolving; SCIP itself is unverified and its 10.0 report is an unrefereed preprint. What the verified members establish is that one accepted proof implies unsatisfiability or the claimed bound. None of them verifies a solver, and a certificate licenses one run rather than the algorithm that produced it. [Flippo, Sidorov, ten Brink, Pit-Claudel and Demirović, *Formally Verified Certification of Constraint Programming Proofs*, CP 2026](https://drops.dagstuhl.de/entities/document/10.4230/LIPIcs.CP.2026.24), [Koops et al., *Practically Feasible Proof Logging for Pseudo-Boolean Optimization*, CP 2025](https://drops.dagstuhl.de/entities/document/10.4230/LIPIcs.CP.2025.21), [Manrique and Szeider, *LeanCSP*](https://arxiv.org/abs/2607.28459), [Szeider, *PBLean*](https://arxiv.org/abs/2602.08692), [Szeider, *Streaming LRAT Certificates into Lean Theorems*](https://arxiv.org/abs/2607.00815), [Cruz-Filipe, Heule, Hunt, Kaufmann and Schneider-Kamp, *Efficient Certified RAT Verification*, CADE-26](https://doi.org/10.1007/978-3-319-63046-5_14), [*The SCIP Optimization Suite 10.0*](https://arxiv.org/abs/2511.18580)

The register decides what follows from that list. R-05-065 is the standing rule: a net-new verified artifact whose only yield is performance on a path already correct and already leak-free is inadmissible. R-05-066 glosses it as targeting *minting a checker* rather than the asymmetric-trust pattern, under which an untrusted producer whose output the already-existing checkers re-validate is admissible and free to be arbitrarily aggressive; its criterion states where that permission stops, an optimizer being admissible iff its output is decided by an existing checker with no new checker introduced. An offline placement search here is that pattern already, its candidates decided by the feasibility, representability and lower-bound checks the [scaling experiment](../implementation/static-memory/scaling.md) and the [placement-search contract](../implementation/placement-search.md) run, and it needs no certificate-checking import in order to search hard. Importing one of these systems to decide those candidates crosses that criterion: it is a new checker, so the search it serves no longer satisfies the clause R-05-066 admits a search under. Two further rules reach the same import. R-05-104's criterion says no ILP machinery exists in the toolchain, so the linear-programming family is deleted rather than retargeted. R-05-105's no-tightening rule reaches a certified-optimal layout over a plan that is already sound and already admitted, whose yield is precisely a smaller span, and that rule takes the trivial sound bound instead; R-05-106 records that this is one rule rather than a second judgment, reaching an artifact as readily as an estimate, so nothing turns on whether the yield arrives as a tool or as a number. Anything surviving those would still owe R-05-020, which admits a new semantics, program logic or translator only on a shown demonstration of three conditions: Coq-native or mechanically bridged, non-duplicating of an existing anchor, and retiring an interim it replaces. CakePB, LeanCSP, PBLean and the Lean LRAT route check in HOL4 or in Lean and so fail the first condition outright until someone builds and shows the bridge, and none of these candidates retires an interim.

One argument in this family clears the tightening ground and is refused on another, and the two are easy to conflate. A checked infeasibility certificate would change a verdict rather than sharpen a number: it turns "the search found no placement" into "no legal placement exists for this arena under these constraints", which the trivial sound answer does not supply, so R-05-105 and R-05-106 do not reach it. R-05-066's criterion does, and independently of any yield, because the route in question is an untrusted search whose candidates a newly imported checker decides. Admitting it would mean changing that criterion, not qualifying under it, and a demonstration would still owe R-05-020's three conditions in full. Two of the candidates above are Coq-native, which is that requirement's first condition under the spelling predating Rocq: FznDrcpCheck, proved in Rocq, and the Coq LRAT checker. The LRAT checker's terms are unread, being the bare tarball recorded above, so nothing about incorporating it is decidable until those terms are read. FznDrcpCheck is the one whose licence, venue and trusted base are all read, and that trusted base is the point: the FlatZinc grammar carried in its specification, and the Rocq-to-OCaml extraction and compiler, are trusted rather than proved, so what an import grows is the trusted base at exactly the point where a capacity refusal is read. The standing position is unchanged: an untrusted search whose candidates the existing checks decide.

R-08-012b also excludes sampled profiles, learned weights, and runtime feedback from placement. Research traces may evaluate a candidate but cannot silently become production placement inputs. R-08-018b separately permits a workload size histogram to inform the composition-fixed size-class set; that is a distinct input with a distinct purpose.

The optional [portable LRAT experiment](../implementation/static-memory/certificates.md) invokes a separately licensed Isabelle-LLVM checker as evidence-producing machinery under R-05-011b and R-15-094. Its external verdict grounds no admitted placement, infeasibility or refinement claim and supplies no instance-specific Rocq term. It does not meet the [placement disposition](architectural-alternatives.md#certified-placement-and-infeasibility-no-new-certificate-machinery)'s reopening conditions; the production refusal remains unchanged.

Q5's present enumerator ranks span and then padding while holding lifetimes fixed. Joint scheduling is a proposed extension that changes the emitted artifact and its live load; it is not a capability of that base enumerator.

TensorFlow Lite Micro is a useful engineering control: its documented arena distinguishes reusable head buffers, temporary storage, and persistent tail allocations. Measure all of them when reproducing its approach. A planned tensor head alone is not a whole operating system's memory budget. [TFLM memory-management documentation](https://github.com/tensorflow/tflite-micro/blob/main/tensorflow/lite/micro/docs/memory_management.md)

Apache TVM's unified static memory planning is a second engineering control, and what it offers is an interface rather than an algorithm. Its accepted RFC reduces every allocation across a whole program to a buffer size, a conflict set, an alignment and a set of candidate pools, then rewrites allocations into offsets in named workspace and constant pools; the planner behind that interface is one of three heuristics, selected by a pass configuration and opt-in. It states no optimality theorem and no non-overlap argument of any kind, which is precisely why it is a shape for an exporter interface and not evidence about a bound. It is also version-dependent: it shipped in the ahead-of-time executor path, and it fell with that path's C++ components, present at v0.19.0 and gone from v0.20.0 onward, so this reading is of the accepted RFC and of the v0.19.0 tree. Compare it with Q5's exporter for what a planner is told, not for what a planner concludes. [The Unified Static Memory Planning RFC](https://github.com/apache/tvm-rfcs/blob/main/rfcs/0009_Unified_Static_Memory_Planning.md)

### Regions, resource types, and explicit phase structure

**Published language research.** Spegion supplies implicit non-lexical, splittable regions, sized allocations, and a type-safety result. It offers a concrete basis for studying earlier region release and bounded subregions. Less lexical retention may improve capacity while making lifetimes less laminar and physical placement harder; both effects need measurement. Its source-language theorem does not supply CHERI-TAL lowering, quarantine, DMA ownership, or whole-image reservations. Its progress and preservation proofs are pen-and-paper, in no proof assistant, and it ships no implementation and no artifact, so it is a calculus to read and adapt rather than a component to reuse. [Hughes, Vollmer and Batty, *Spegion*, ECOOP 2025](https://drops.dagstuhl.de/entities/document/10.4230/LIPIcs.ECOOP.2025.15)

**Published resource analysis.** RaRust uses resource-aware types and prophecy potentials to infer linear resource bounds through Rust borrowing. This is evidence that ownership and quantitative resource reasoning can be combined, not that ordinary ownership already proves a bound. A transfer needs a cost semantics that counts peak resident storage and delayed reuse, not merely accumulated allocation or abstract ticks. Its own soundness proof is on paper, its inference is incomplete, and its bounds are linear and in a user-chosen tick metric. The same amortized-analysis line has a longer implementation record in Resource Aware ML, which is no more mechanized: no proof-assistant development connects that tool to its paper theorems, its newest release is a 2020 tarball rather than a repository, and the tarball bundles example code under the INRIA Non-Commercial License and an OCaml compiler front end under the Q Public License, so RaML's own files being BSD 2-Clause does not describe what vendoring the distribution would import. [Lian and Wang, *Automatic Linear Resource Bound Analysis for Rust via Prophecy Potentials*, OOPSLA 2025](https://stonebuddha.github.io/publication/lianw25/), [Resource Aware ML](https://www.raml.co/about/)

**Published mechanized space bounds, idealized models.** Two Coq developments mark the difference between a proved space guarantee and an inferred one. IrisFit is a separation logic with space credits, built on Iris for a concurrent calculus with a tracing collector, and its machine-checked results are that a verified program cannot exceed a fixed heap-space limit and that, given enough polling points, every allocation request is satisfied within a bounded number of steps. Those bounds are the ones a user states and proves, the collector and the calculus are idealized, and only heap space is measured: stack and time are not. memmgr is the closest published analogue here to an unconditional allocation guarantee, being a sequential malloc and free verified in VST against a separation-logic specification, with a resource-aware specification saying when allocation succeeds rather than permitting a null return at will, and a proof that the resource-aware specification implies the resource-oblivious one. It is sequential, its memory source is axiomatized at the boundary, and its guarantee is stated over CompCert C semantics. Both results come from a premise someone discharged, which is the same boundary the resource-type reading draws: a guarantee is proved, never implied by ownership. memmgr's terms are LGPLv3 from the repository it actually lives in, not the permissive terms of the toolchain it is usually attributed to, which decides reuse of its code and not reading of its specification. [Moine, Charguéraud and Pottier, *Will it Fit? Verifying Heap Space Bounds of Concurrent Programs under Garbage Collection*, TOPLAS 47(1), Article 3, 2025](https://doi.org/10.1145/3716312), [Appel and Naumann, *Verified sequential Malloc/Free*, ISMM 2020](https://doi.org/10.1145/3381898.3397211)

There is a local policy boundary: R-05-105 rejects a verified tool whose only yield is tightening an already sound bound. A research analyzer can diagnose retention and guide transformations. Admission of a new analysis requires another demonstrated yield or an explicit policy revision; a tighter inferred number alone is not an adopted lever. Under the current rules, prioritize changing the program into bounded phases whose existing certificate exposes the improvement.

### Recomputation, streaming, and representation changes

**Established optimization, workload-dependent transfer.** Checkmate formulates tensor rematerialization as a computation-versus-memory optimization and emits a static schedule. Adapt its method to pure computations whose inputs remain available: recompute an intermediate, tile a frame, stream a parser, or fuse adjacent stages so less data stays resident. Keep irreversible I/O, nonce consumption, and mutable observations outside replay unless a functional proof permits it. Use admitted worst-case costs and traffic, not the paper's profiled accelerator costs. [Jain et al., *Checkmate*, MLSys 2020](https://proceedings.mlsys.org/paper_files/paper/2020/hash/0b816ae8f06f8dd3543dc3d9ef196cab-Abstract.html)

**Published structural optimality, no solver.** Where the recomputation problem carries enough structure, an optimal schedule follows from dynamic programming and no solver is involved. CAMS derives checkpointing schedules for the discrete adjoints of multistage time-stepping schemes and argues optimality, fewest recomputations for a fixed step count and a fixed number of checkpoint slots, in a cost model counting recomputations and stage stores rather than wall-clock time; the argument is pen-and-paper, neither the argument nor the library implementing the schedules is machine-checked, and the reported speedups are measured. The transferable part is the shape of that result and not its schedule: an exactly optimal recomputation plan exists for a problem whose structure is fixed in advance, which is the regime admitted frames and declared phases impose here, so a structural restriction is what to look for before any exactness claim is reached for. [Zhang and Constantinescu, *Optimal Checkpointing for Adjoint Multistage Time-Stepping Schemes*, Journal of Computational Science 66:101913, 2023](https://doi.org/10.1016/j.jocs.2022.101913)

R-08-019e already admits recomputation and changes to the committed occupancy limit. The research contribution is a checked transformation and a useful frontier of bytes, cycles, bandwidth, code size, and energy. Repeatedly streaming weights from another machine changes the resident-inference contract; it is not an uncharged version of local recomputation. Reducing an occupancy promise or evicting state is a service change, reported separately from an equivalent implementation's capacity gain.

### Bounded pieces instead of mandatory contiguity

**Very recent theoretical lead.** Bender et al.'s August 2026 submission studies *request fragmentation*, meaning deliberately splitting an allocation into pieces. For constant aggregate factor `k > 1`, it reports an optimal online ratio `Theta(log log Mbar)` when the actual peak live volume is unknown and bounded above by `Mbar`. The budget compares all-time peak simultaneous fragments with all-time peak simultaneous requests. A fixed small segment limit per object is a different model and does not inherit that result; knowing the actual peak volume also changes the bounds. This is a preprint whose own comments field still reads *in submission*, so it carries no venue and no completed peer review, and "Bender et al." abbreviates six authors. It is about online allocation, not a theorem for fixed offline slots. [Bender, Conway, Farach-Colton, Komlós, Kuszmaul and Wein, *Tight Bounds for Memory Allocation With and Without Request Fragmentation*](https://arxiv.org/abs/2608.28462)

The plausible local experiment is narrower: compile selected arrays, queues, or object collections into a bounded set of fixed chunks within one owner. Each chunk has its own valid capability and declared slot; no page-table translation is necessary. Bound descriptor count, tail slack, indexing, tag traffic, revocation, and DMA/vector access costs. A contiguous external ABI can force copying and erase the saving. Allowing the runtime to choose arbitrary chunks from a shared free set would add online placement and needs a separate architecture decision. The paper motivates exploring representation freedom; it supplies neither this implementation nor its proof.

### Reclamation as a capacity scheduling problem

**Existing mechanism, open composed cost.** CHERIoT's documented heap quarantine illustrates why logical deallocation and physical reuse differ. Its allocator and revoker are not implementations of this repository's software sweep and fixed schedule. Here the controlling obligations are [containment and reuse](../spec.md#r-08-007a), [bounded sweep service](../spec.md#r-08-007), and [the unmeasured teardown rate](../spec.md#r-08-008a). [CHERIoT memory guide](https://cheriot.org/book/memory.html)

A research model can describe release arrivals by a worst-case envelope `alpha(T)`, the maximum charged physical extents entering release-to-reuse in any interval of length `T`, including slot padding. If every release becomes reusable within a proved bound `T_reuse`, unreusable released storage is bounded by `alpha(T_reuse)` after the initial backlog clears, with a declared order for events at the same timestamp. Count Quiescing retention before containment as well as quarantine after it. Derive `T_reuse` from containment, outstanding authority, complete sweep coverage, and initialization service; do not substitute an average free rate. Optimize sweep slots and reserved capacity together, accounting for their feedback and each pool's service guarantees. A faster sweep is not free memory if it removes bandwidth required by the admitted workload.

## Exploratory branches that change assumptions

| Branch | Potential benefit | Boundary and decisive experiment |
| --- | --- | --- |
| Public, predeclared phase overlays | Reuse the same addresses for different objects when the earlier phase is completely dead | Same-owner reuse with proved non-overlap is already available under R-08-018a. Extending it to new runtime modes or owners needs transition, residency and authority proofs; inactivity alone does not kill saved application state |
| Bounded handle-based relocation | Compact movable state without an MMU by resolving a stable typed handle through bounded software metadata | R-08-020 forbids runtime relocation. Measure metadata, copy workspace and worst-case pause, and account for every direct capability, interior pointer, saved register, sentry and device loan; a handle indirection alone does not revoke escaped pointers |
| Cross-owner capacity lending | Use slack where another owner needs it | R-08-012c and R-08-047 forbid moving island boundaries and borrowing. Prove a new ownership transition, reserve return capacity, and specify whether denial or timing reveals the lender's private demand |
| Runtime value deduplication or compression | Recover capacity that static contents and occupancy analysis cannot predict | R-08-019a names this residue. Charge equality checks, labels, dictionaries, expansion reserves and worst-case decode; cross-owner equality and success can disclose data |
| Recomposition at reboot | Redistribute reservations for a different installed workload | Fits the generation model; quantify downtime and migration workspace. It cannot rescue the current generation's full pool during execution |

The phase experiments concern data. [Executable overlays](architectural-alternatives.md#static-code-overlays-a-deterministic-instruction-scratchpad-deferred-until-a-measured-resident-image-fails-the-capacity-budget) have their own deferral under R-15-100b and reopen loader and executable-memory obligations. Likewise, the existing [compression and deduplication disposition](architectural-alternatives.md#transparent-variable-rate-memory-compression-and-runtime-deduplication-declined) governs that table's exploratory branch; naming its potential gain does not admit it.

Two outside comparators sharpen the boundary. Mesh uses virtual-memory operations to compact physical backing while preserving C/C++ addresses, so importing its central mechanism would undo the no-MMU choice. Recent reallocation theory studies space and expected normalized movement cost with live-object relocation allowed; that does not establish a worst-case pause for a fixed-slot machine. Jin's published improvement sharpens that line to worst-case expected overhead `polylog(1/epsilon)` per update, and the parameter is the boundary: the model holds live objects to at most a `1 - epsilon` fraction of memory, so the guarantee weakens exactly as the arena fills, a gap to the `Omega(log 1/epsilon)` lower bound remains, and the paper separately proves that a polylogarithmic worst-case bound holding with high probability is unattainable. The guarantee is therefore an expectation in a model that may move live objects, the movement R-08-020 forbids. A better expected movement cost is not a shorter worst-case pause, and this design charges the pause. [Powers et al., *Mesh*, PLDI 2019](https://people.cs.umass.edu/~mcgregor/papers/19-pldi.pdf), [Farach-Colton et al., *A Nearly Quadratic Improvement for Memory Reallocation*, SPAA 2024](https://arxiv.org/abs/2405.12152), [Jin, *Memory Reallocation with Polylogarithmic Overhead*, STOC 2026](https://doi.org/10.1145/3798129.3800822)

A third comparator is nearer still, and its attraction is exactly what disqualifies it. Silva et al. prove on paper that a constant-bounded program, one whose execution length is syntactically limited for every input, can be allocated a stack of at most its maximum live memory, the `L` of the model above, plus a defragmentation scratch region bounded by the size of its largest variable, computed in polynomial time by treating control flow as a tree and scanning it; equality with that maximum holds only where in-place swaps are available. Both the scratch region and the equality case rest on moving objects during execution, which R-08-020 forbids, so the bound shares the changed assumption of the handle-based relocation branch above and is not an offline placement result. The bound also holds only after loop unrolling, treeification and dead-code elimination, and its defragmentation can expand code exponentially in branch count; the reported stack reductions are measurements on eBPF and bounded MLIR workloads rather than proved quantities. It is a preprint with no venue, nothing in it is mechanized, and the implementation it pins is the eBPF compiler it extends, under GPLv3: decisive for any reuse of that code, and not for reading the theorem. [Silva, Soares, Costa and Quintao Pereira, *Memory Allocation for Constant-Bounded Programs*](https://arxiv.org/abs/2608.14471)

The absence of an MMU is not the reason every movable design fails. Software handles, copying, and typed reconstruction are possible design choices. They add runtime work and authority transitions which the present design excludes. Study their cost explicitly if the offline approaches leave a demonstrated capacity failure.

## Open questions and falsifiable hypotheses

**A lower bound to keep honest.** Suppose `n` owners each require an unconditional reservation of `B` bytes, reservations cannot move between owners, and any owner may be selected to run. Each owner still needs its own `B` even if an external service rule permits only one active owner at once. Total reservation is at least `n * B`, whereas that rule's peak active payload is `B`. This is an elementary consequence of those premises, not an open conjecture. Fixed public phases with reusable ownership, weaker service guarantees, or runtime transfer change a premise. An optimizer cannot remove this cost while claiming all premises unchanged.

**Hypothesis: restricted lifetime structure can make exact planning practical.** Start with positive integer object sizes and lifetime endpoints encoded in binary, one arena, unit alignment, every nonnegative integer base legal, and no pinning. Let `k` be the minimum number of intervals whose removal makes the actual lifetime family laminar. Can exact placement be solved in time `f(k) * poly(input length)`, without dependence polynomial in the numerical address-space size, or is it hard even for a small fixed `k`? This is a proposed parameterized-complexity question. Small enumerations can expose counterexamples to candidate decompositions; they cannot establish the complexity claim. Non-unit alignment, restricted positions, owner and bank constraints need separately stated variants and input encodings.

**Hypothesis: bounded segmentation buys useful capacity after its costs.** For at least one representative service with variable-sized state, an equivalent implementation using a composition-fixed segment limit admits a strictly larger demand envelope at the same physical capacity and no worse admitted deadlines than its contiguous baseline. Falsify the hypothesis for the selected workload if descriptors, padding, copy staging, code growth or bank traffic consume the improvement. This is an empirical claim about an identified service, not a universal approximation theorem.

**Open proof problem: compositional reuse under public phases.** Can per-component phase certificates establish global non-overlap and bounded reuse without enumerating every combination of component states? A useful theorem would compose fixed phase schedules with asynchronous completion, retained state, and the complete authority-revocation barrier. A counterexample in which a late device completion crosses a reuse boundary refutes a proposed rule. Secret-dependent phase selection additionally needs an explicit leakage model; public labels alone do not prove non-interference.

**Open tradeoff: useful slack versus private demand.** State a relational model in which a borrower's acceptance and completion time are observations, while another owner's occupancy is secret. Compare identical borrower requests under different lender demand. A lending policy whose observed result differs violates that model. Determine the minimal reserved headroom, padding, public release rule or deliberate declassification needed for useful sharing. No universal impossibility result is asserted here: the answer depends on which service guarantees and observations are required.

**Hypothesis: reclamation-aware scheduling outperforms placement alone.** On a bursty but bounded service, jointly choosing retirement phases, sweep slots and quarantine reservation admits more useful state than optimizing placement with reclamation costs fixed, at unchanged external service guarantees. Refute it on that workload if the required sweep traffic or worst-case arrival envelope removes the gain. A result showing that quarantine dominates packing is valuable even when no transformation wins.

## Research screened without an implementation workstream

These dispositions concern the present consumers and do not claim that the
underlying mathematics has no other application. Reopening one requires a
specific register obligation and a premise-preserving connection to it.

| Topic | Disposition and reason |
| --- | --- |
| P versus NP | Declined as a replacement security premise. Worst-case `P != NP` is not known to imply the average-case hardness required of cryptographic one-way functions. `P = NP` would defeat their asymptotic definition, even if its first algorithm were impractical at deployed sizes; practical break cost would still need a concrete analysis. Neither outcome is the present per-primitive assumption ledger. [Goldreich, *Computational Complexity*, chapter 7](https://www.wisdom.weizmann.ac.il/~/oded/CC/r3.pdf) |
| Williams' time-to-space simulation | No current implementation workstream. The theorem simulates a time-`t` multitape Turing machine in `O(sqrt(t log t))` space; it supplies no target WCET, CHERI refinement or affordable slowdown for an admitted service. The design permits separately admitted recomputation, so the reason is the missing bounded target implementation, not a blanket refusal of time/space tradeoffs. [Williams, STOC 2025](https://arxiv.org/abs/2502.17779) |
| Hilbert's tenth problem over number-field integer rings | No additional consumer identified. The number-field extension concerns solvability of unbounded polynomial equations over those rings; it does not supply a decision method or a stronger applicable limit for the platform's finite admission arithmetic. Integer-fragment restrictions belong to CHERI-TAL's own contract. [Alpöge, Bhargava, Ho and Shnidman, 2025 preprint](https://arxiv.org/abs/2501.18774) |
| Sensitivity conjecture | Declined for this scope. The proved relation concerns Boolean-function complexity; no reduction from a placement, entropy or admission obligation is supplied. [Huang, 2019](https://arxiv.org/abs/1907.00847) |
| Bunkbed disproof | Declined for this scope. A counterexample to a percolation conjecture supplies no correction or independence theorem for this machine's fault model. [Gladkov, Pak and Zimin, 2024](https://arxiv.org/abs/2410.02545) |
| Erdős-Faber-Lovász | Declined for this scope. The theorem for sufficiently large linear hypergraphs does not establish contiguous weighted placement, timing admission or their small-instance guarantees. [Kang, Kelly, Kühn, Methuku and Osthus](https://arxiv.org/abs/2101.04698) |
| Good locally testable codes | Watch list only. Constant rate, distance and local testability do not qualify a concrete fixed-latency correction datapath or its detection/fail-stop scope. Any proposal must identify code length, error model, decoder and timing before changing the ECC contract. [Dinur, Evra, Livne, Lubotzky and Mozes](https://arxiv.org/abs/2111.04808), [Panteleev and Kalachev](https://arxiv.org/abs/2111.03654) |
| Reed-Solomon list-decoding capacity | Watch list only. Capacity results for randomly punctured codes are combinatorial guarantees; they do not supply the platform's bounded decoder, a unique authenticated result, or an admitted worst-case latency. [Alrabiah, Guruswami and Li](https://arxiv.org/abs/2304.09445) |

## Desktop planner integration

The [portable planner](../implementation/portable-memory-planner.md) implements a
host boundary for fixed-instance placement with independently checked candidates,
a retained valid baseline, deterministic search budgets and separate evidence.
It covers bounded services as well as tensor workloads and records the exact
scope of its MiniMalloc, ExecuTorch and TFLite Micro adapters. Model compatibility,
API compatibility and acceptable deployment cost are distinct claims. Its
contract extractor strengthens the modeled coexistence relation; connecting
that relation to admitted Vela programs and actual device completion remains a
compiler and qualification obligation. Q5b owns the real-workload comparison,
Q22 the reuse boundaries and Q10 the product judgment.

The [static-memory literature disposition](../implementation/static-memory/literature.md)
evaluates allocation, certification and reusable-capacity donors entry by entry.
It links actual upstream candidate/checker integrations separately from adapted
resource-contract ideas, existing experiments and alternatives whose premises do
not match fixed backing. A cited theorem is credited only at its stated endpoint;
Q5b and Q22 retain the source, target and workload joins.

## Research todo list

This backlog tracks a research project, not an additional implementation schedule. Work selected for execution is scoped in the [implementation checklist](../implementation/implementation-checklist.md); Q5 owns placement comparisons, Q4 inference demand, Q6 memory topology, Q8 measured bottlenecks, Q22 assurance boundaries, and Q10 the product judgment. The research reuses those artifacts and returns findings to their owners. Research outputs do not confer implementation landing credit or accept a new requirement.

The [replayable first experiments](../implementation/static-memory/experiments.md)
provide a mathematical baseline, synthetic corpus, diagnostic ledger and bounded
placement comparison. Each checkbox applies to its stated deliverable: a reviewed
baseline can close while its compiler implementation remains open. The unchecked
items retain their full acceptance conditions; a witness implementation does not
complete an item's product, proof or generality obligations.

The [worked replay walkthrough](../implementation/static-memory/experiments.md#walkthrough-three-kinds-of-unavailable-capacity)
includes diagnostics for owner isolation, separated free
extents and delayed device completion, with receipt fields that distinguish free
backing from a usable declared slot and completed reuse.

- [x] **Define the mathematical and operational baseline.** Produce a reviewed model of payload liveness, retained authority, safe reuse, legal placement, and per-arena physical charge. Reconcile R-08-011 through R-08-013 with the exact theorem assumptions and the placement-search quantities. Deliver a proof or minimal counterexample for each disputed implication before optimizing it.
  Completion evidence: the [reviewed disposition](../implementation/static-memory/baseline.md#baseline-research-disposition) maps each disputed implication to a proof or counterexample. The [operational interface](../implementation/static-memory/baseline.md#the-operational-interface-a-producer-must-satisfy) states the binding, interference and completed-reuse premises and proves conditional isolation and occupancy bounds. R-08-011 through R-08-014 distinguish those premises from ownership alone, state the charged-load inequality and scoped laminar theorem, and use the correct interference predicate. The [mode comparison](../implementation/static-memory/modes.md) separates per-mode optima from a common layout. Producing sound intervals for actual admitted programs and implementing the qualified reuse gate remain downstream obligations.
  The [mode-family witnesses](../implementation/static-memory/modes.md#the-declared-witnesses) separately exercise live state at the source and destination of a switch. A destination that is already live refuses the binding family even when the source is dead; the refusal carries no binding charge or optimality claim.
- [ ] **Build a reproducible capacity corpus.** Start from Q5's exporter and the real composed roster when available. Include bounded parsers, sessions, rings, saved application state, frame pipelines and inference, plus synthetic crossing lifetimes, adversarial sizes, burst teardown and delayed device completion. Record source revision, manifest, all cost assumptions and demand envelope. Keep witness demonstrations labeled as such.
  Current output: the [corpus and Q5 bridge](../implementation/static-memory/corpus.md) emit identified synthetic contracts for every named family, cost assumptions, demand series and computed coverage. A separated-free-extents witness distinguishes total free bytes, contiguous free geometry and declared slot size. A same-owner size-class witness shows a larger request refused despite enough contiguous idle backing, then fitting its declared slot at completed reuse. The bridge reports absent source fields; no contract comes from an actual composed roster carrying per-component memory extents.
  The [alignment-stranding witness](../implementation/static-memory/corpus.md#geometric-fit-and-typed-refusals) isolates an incompatible slot base from insufficient space: relaxing only the request alignment changes refusal to an instantaneous fit.
  The `same-owner-arena-stranding` witness isolates placement from ownership: an idle declared slot in another arena fits the same request, while the requested arena refuses it until its own slot completes reuse. Neither result permits moving capacity between arenas.

  - [x] Build and replay the [synthetic capacity corpus and Q5 bridge](../implementation/static-memory/corpus.md#synthetic-corpus-disposition), covering every named family with source and manifest identities, explicit cost assumptions, bounded demand traces and witness labels. Preserve Q5's missing operational inputs; real-roster contracts remain open.

- [ ] **Implement a diagnostic byte ledger.** For each owner, arena and admitted mode, report useful payload, reserved span, disjoint overhead charges, occupancy, unreusable retired bytes, largest legally usable extent and typed refusals. Preserve telemetry labels. Derive every figure from artifacts; measure equivalent service and alternative service contracts separately.
  Current output: the [event ledger](../implementation/static-memory/corpus.md) partitions bytes and diagnoses bounded requests under explicit witness premises. Target telemetry and compiled-service cost extraction remain open.

  - [x] Validate the [finite-trace byte ledger and request diagnostics](../implementation/static-memory/corpus.md#finite-trace-ledger-disposition): independently check disjoint charges at every fixture tick, distinguish free geometry from idle declared slots, and preserve source identity, telemetry, cost, reuse and service-contract labels on every fit and typed refusal.

- [x] **Establish exact small-instance oracles.** Exhaustively enumerate small layouts and use independently checked lower bounds to separate unavoidable fragmentation from planner error. Mutate lifetimes, alignment, owner assignment, slot overlap and reuse barriers; invalid candidates must fail. A feasible certificate and an optimality certificate are different deliverables.
  Completion evidence: the [exact oracle and independent replay](../implementation/static-memory/experiments.md), [representability-granule layer](../implementation/static-memory/representability.md) and [span lower bounds](../implementation/static-memory/scaling.md) separate checked feasibility from optimality over the declared finite model. The [mutation sweep](../implementation/static-memory/mutants.md) rejects the named placement defects, reuse before each completion barrier and internally consistent feasible-but-suboptimal certificates; independent byte/tick checks and refusal controls exercise those distinctions. Incomplete searches and stillborn mutants supply no successful verdict. Remaining capability-format rules and extraction of actual authority-completion evidence are extensions beyond this finite oracle.
- [ ] **Scale offline planning.** Benchmark deterministic heuristics and coupled search against the existing enumeration and exact oracles. Report best feasible span, proved lower bound, remaining gap, build time and reproducibility. Preserve the standing plan on timeout or failure to find improvement. Introduce and license-review dependencies only when that comparison justifies them.
  Current output: the [scaling experiment](../implementation/static-memory/scaling.md) compares checked heuristics, bounded exact search and the existing Q5 witness. Each arena reports its strongest established lower bound, best checked span, remaining gap and work completeness. Interrupted lowering retains a partial improvement as diagnostic evidence while its selected placement and arena summaries keep the standing plan. A distinct coupled-search candidate remains to be benchmarked against those references. Applying the comparison to the actual roster and target constraints remains Q5b work.

  - [x] Establish [reproducible synthetic heuristic benchmarks](../implementation/static-memory/scaling.md#replay-and-receipt) with exact-oracle and Q5 comparisons, per-arena spans, lower bounds and gaps, identified host timings, and standing-plan preservation under work-budget exhaustion. Coupled-search comparison, actual-roster constraints and target build measurements remain open.

- [ ] **Prototype lifetime and representation transformations.** Compare lexical regions, explicit bounded phases and selected non-lexical release; compile one service both contiguously and into fixed bounded chunks. Prove result equivalence and account for aliases, capability narrowing, descriptors, vectorization and DMA staging. Record whether each gain is due to less live data or better placement.
  Current output: the [frame-service experiment](../implementation/static-memory/transformations.md) compares retention, early release, chunks and rematerialization, with modeled descriptor and staging charges. [StaticMemoryService.v](../../proofs/StaticMemoryService.v) proves the list-functional models equivalent under stated partition premises. Refinement of the emitted programs, authority and DMA behavior remains open.

  - [x] Complete the [finite frame-service transformation prototype](../implementation/static-memory/transformations.md#finite-prototype-disposition): compare retention, phases, early release and bounded chunks under one configured service, with independent output, erasure, bounds, placement and disjoint-byte checks. Emitted-program refinement and target alias, narrowing, vectorization and DMA obligations remain open.

- [ ] **Explore the bytes-versus-work frontier.** Generate static rematerialization, tiling and fusion variants. Derive worst-case execution and traffic from the emitted code, including zeroization and code-size growth. A candidate closes only if the same service fits its original deadlines, memory classes, power constraints and admitted image.
  Current output: the [transformation frontier](../implementation/static-memory/transformations.md) counts abstract work and traffic, and the [compiled census](../implementation/static-memory/census.md) derives two kernels through Rupicola and a contained compiler. Its plain-RV64 instruction counts are a proxy; target WCET, image size, power and composed deadlines remain unqualified.
- [ ] **Co-design reclamation and capacity.** Derive worst-case retirement envelopes and complete release-to-reuse bounds. Exercise saved-register retention, grant loans, stalled endpoints, restart storms and stale capabilities behind the sweep cursor. Compare quarantine bytes and service availability across fixed sweep schedules, using Q22's boundary obligations.
  Current output: the [reclamation experiment](../implementation/static-memory/reclamation.md) derives calendar terms from Q22a holder signatures and compares fixed schedules. The [adversarial envelope](../implementation/static-memory/envelope.md) supplies declared retirement budgets, release-to-reuse bounds and bounded search checks. Actual holder coverage, admitted deadlines and qualified service rates remain open.
  The [reclamation walkthrough](../implementation/static-memory/reclamation.md#receipt-walkthrough) follows the generated receipt from the baseline and joint schedules to traffic refusals and failed reuse. It distinguishes recovered backing from peak quarantine and shows why failed slots remain charged.

  - [x] Validate the [finite reclamation calendar and adversarial envelope](../implementation/static-memory/reclamation.md#finite-reclamation-research-disposition), with declared retirement budgets, fixed-schedule capacity comparisons, conditional reuse bounds, independent byte/tick checks and Q22a refusal witnesses. Actual holder coverage, admitted workloads, target service rates and all-executions proofs remain open.

- [ ] **Prove or refute the structural questions.** Formalize the laminar special case and the restricted-lifetime parameterization, then characterize which added constraints break them. Publish negative results and minimal witnesses as first-class outputs. A mechanized feasibility check does not close the algorithmic optimality problem.
  Current output: the [mechanized laminar statement](../implementation/static-memory/baseline.md#mechanized-statement) proves the scoped placement theorem and a crossing-family gap. The [deletion-parameter study](../implementation/static-memory/structure.md) supplies a polynomial parameter computation, a bipartite crossing-graph construction and refuted decompositions. The source-lifetime bridge, general replay correspondence and parameterized complexity question stay open; the following narrower structural deliverables are complete.

  - [x] Prove the [sharp triangle-free gap threshold](../implementation/static-memory/structure.md#a-sharp-gap-threshold-for-triangle-free-crossing-graphs): deletion number three permits a gap, and smaller deletion numbers cannot in that graph class. The unrestricted threshold is narrowed to `{2, 3}`.
  - [x] Replay the explicit gap witness with independent feasibility and optimality checks, and establish inclusion-minimality by checking every proper subfamily. This does not establish the least object count over all possible witnesses.
  - [x] Prove the [extent-gcd lattice lemma](../implementation/static-memory/structure.md#an-exact-search-without-address-enumeration) and use it in exact search, including common scaling, capacities between lattice points and incomplete-search checks. This is not a parameterized complexity result.

- [ ] **Isolate one architecture experiment if justified.** Select public phase reuse, typed relocation, or capacity lending only after baseline measurements show the limitation it addresses. Keep an explicit changed-assumption ledger and prove the transition's authority, timing and information-flow claims. Reject a candidate with an unbounded pause, hidden reserve, or unreconciled exclusion.
  Current output: the [public-phase model](../implementation/static-memory/phases.md) tests compositional reuse certificates, while the [lending model](../implementation/static-memory/lending.md) separates refusal and timing channels with two-run witnesses. Both carry explicit changed assumptions and finite scope. Neither establishes a qualified product limitation or selects a new admission rule; cross-owner lending remains excluded.
- [ ] **Run a composition-level comparison.** Feed promising variants into Q5b and the product gate on the same roster and functional contract. Report which additional workload is admitted, all resource deltas, exhaustion behavior and any weakened promise. Perform ablations so packing, lifetime changes, segmentation and reclamation do not each claim the same recovered bytes.
  Current output: the reclamation and transformation reports compare declared ablations, the compiled census supplies a proxy, and the scaling report retains the existing Q5 witness. M7.1's actual roster, compiled service contracts and qualified product resource inputs remain required before Q5b can credit additional admitted workload.
- [ ] **Publish a reproducible research artifact and disposition.** Release definitions, proofs, generators, minimized counterexamples, checker inputs, search settings and replayable measurements. Identify peer-reviewed results, preprints, new conjectures and measured outcomes separately. Propose register changes only with the precise theorem or measured contract that replaces the current claim; an inconclusive experiment remains open.
  Current output: the [artifact inventory](../implementation/static-memory/artifact.md) computes the files and registered actions, checks completeness and classifies the results. Replay reports malformed experiment and research receipts as findings, including when the action exits successfully; research receipts require matching action identity and nonempty contract-bearing cases. Expected diagnostic refusals and incomplete searches retain their stated scope. The [replay guide](../implementation/static-memory/experiments.md) binds source identities and inputs. External publication, peer review, the remaining mechanized refinements and a measured product comparison stay open.
  Experiment receipts also require matching action identity and a nonempty scope. The replay CLI refuses an explicit search budget on an action that does not consume it.

  - [x] Validate the [local research inventory and registered-action replay](../implementation/static-memory/artifact.md#local-artifact-disposition), including indexed sources, completeness, classification, document reachability and malformed-receipt rejection. Intentional refusals and incomplete searches retain their diagnostic scope; external publication, review, implementation refinements and product comparison remain open.

A credible project combines programming-language semantics, algorithms, real-time systems, capability security, and compiler engineering. Its success criterion is a checked increase in useful admitted work with the costs exposed. A proof that a particular reservation is unavoidable, or that a tempting optimization breaks isolation, is also a substantive result.
