diff --git a/plugins/cplugin/certirocq.ml b/plugins/cplugin/certirocq.ml
index 0d9829b1..7747d3bd 100644
--- a/plugins/cplugin/certirocq.ml
+++ b/plugins/cplugin/certirocq.ml
@@ -267,6 +267,7 @@ let make_options (l : command_args list) (pr : prim list) (fname : string) : opt
 let make_unsafe_passes b =
   let open Erasure0 in
   { cofix_to_lazy = b;
+    inlining = b;
     unboxing = b;
     betared = b;
     inductives_extraction = b }
@@ -287,9 +288,8 @@ let make_pipeline_options (opts : options) =
   let erasure_config =
       Erasure0.({
         enable_typed_erasure = opts.typed_erasure;
-        enable_unsafe = if opts.unsafe_erasure then all_unsafe_passes else no_unsafe_passes;
+        enable_unsafe = (let base = if opts.unsafe_erasure then all_unsafe_passes else no_unsafe_passes in { base with inlining = not (opts.no_inlining) });
         dearging_config = default_dearging_config;
-        inlining = not (opts.no_inlining);
         inlined_constants = get_global_inlinings_mapping ();
         extracted_inductives = Obj.magic opts.extracted_inductives; (* kername and list representation are the same *)
         })
diff --git a/plugins/plugin/certirocq.ml b/plugins/plugin/certirocq.ml
index 667a5364..d642e2b4 100644
--- a/plugins/plugin/certirocq.ml
+++ b/plugins/plugin/certirocq.ml
@@ -262,6 +262,7 @@ let make_options (l : command_args list) (pr : prim list) (fname : string) : opt
 let make_unsafe_passes b =
   let open Erasure0 in
   { cofix_to_lazy = b;
+    inlining = b;
     unboxing = b;
     betared = b;
     inductives_extraction = b }
@@ -282,8 +283,7 @@ let make_pipeline_options (opts : options) =
   let erasure_config =
       Erasure0.({
         enable_typed_erasure = opts.typed_erasure;
-        enable_unsafe = if opts.unsafe_erasure then all_unsafe_passes else no_unsafe_passes;
-        inlining = not (opts.no_inlining);
+        enable_unsafe = (let base = if opts.unsafe_erasure then all_unsafe_passes else no_unsafe_passes in { base with inlining = not (opts.no_inlining) });
         dearging_config = default_dearging_config;
         inlined_constants = get_global_inlinings_mapping ();
         extracted_inductives = Obj.magic opts.extracted_inductives; (* kername and list representation are the same *)
