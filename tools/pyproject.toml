# SPDX-License-Identifier: Apache-2.0
[project]
name = "verifiedos-tools"
version = "0"
requires-python = ">=3.14,<3.15"
dependencies = ["jsonschema"]

[dependency-groups]
dev = ["ty==0.0.82", "ruff==0.16.8"]
model = ["pre-commit==4.6.2"]

[tool.uv]
package = false
required-version = "==0.12.17"
python-downloads = "never"
