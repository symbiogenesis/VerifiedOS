# SPDX-License-Identifier: Apache-2.0
[project]
name = "verifiedos-tools"
version = "0"
requires-python = ">=3.14,<3.15"
dependencies = ["jsonschema"]

[dependency-groups]
dev = ["ty==0.0.75", "ruff==0.16.5"]

[tool.uv]
package = false
required-version = "==0.12.12"
python-downloads = "never"
